# Herble

This is Herble backend API service. It includes:
- user and token management
- notifications

### Development

To build: 

`./mvnw clean compile`

To run:

`./mvnw spring-boot:run`

To format:

`./mvnw spotless:apply`

